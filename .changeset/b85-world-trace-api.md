---
"zod4-mock": minor
---

Add `world.trace(): WorldTrace`, the `createWorld({ trace })` opt-in flag, and the public `WorldTrace` / `TraceNode` / `TraceField` / `TraceEdge` / `TraceResolution` provenance types.
