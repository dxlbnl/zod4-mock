---
"zod4-mock": patch
---

Author fresh TSDoc on every public export and generate `docs/api-reference.md` (and the structured `/docs/api` site view) from it, with a `pnpm docs:check` parity guard wired into `pnpm validate`.
