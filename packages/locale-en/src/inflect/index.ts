/**
 * @module @zod4-mock/locale-en/inflect
 *
 * English inflection helpers — re-exported as the `inflect` namespace from
 * `@zod4-mock/locale-en`'s public entrypoint for matcher authors:
 *
 * ```ts
 * import { inflect } from "@zod4-mock/locale-en";
 * inflect.pluralize("city"); // "cities"
 * ```
 */

export * as inflect from "./en.js";
