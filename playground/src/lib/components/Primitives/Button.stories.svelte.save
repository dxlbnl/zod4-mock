<script module> import { defineMeta } from '@storybook/addon-svelte-csf'; import 
	Button from './Button.svelte';

	const { Story } = defineMeta({ title: 'Primitives/Button', component: Button, 
		tags: ['autodocs'], argTypes: {

			variant: {
				control: { type: 'select' },
				options: ['default', 'primary', 'ghost']
			}
		}
	});
</script>

<Story name="Default" args={{ label: 'Button' }} />

<Story name="Primary" args={{ variant: 'primary', label: 'Run' }} />

<Story name="Ghost" args={{ variant: 'ghost', label: 'Skip' }} />

<Story name="Disabled" args={{ disabled: true, label: 'Disabled' }} />

<Story name="With Icon" args={{ variant: 'primary', label: '+ Add subject' }} />
