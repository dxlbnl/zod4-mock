<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import Radius from './Radius.svelte';

	const { Story } = defineMeta({
		title: 'Foundations/Radius & shadows',
		component: Radius
	});
</script>

<Story name="Radius & shadows" />
