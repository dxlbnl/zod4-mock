<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import Syntax from './Syntax.svelte';

	const { Story } = defineMeta({
		title: 'Foundations/Syntax colors',
		component: Syntax
	});
</script>

<Story name="Syntax colors" />
